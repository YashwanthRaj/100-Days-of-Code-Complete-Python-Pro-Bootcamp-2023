Calculator Program
The code takes in numbers and the desired operation. Once completing the operation, it asks if we want to continue and if yes then it repeats the steps. 


return -> used inside function to give out the result value, marks the exit of a function
	    return without specifying any value will just return nothing, this is used if we want to return null value

docstring -> The comments that we enter between '''      ''''. This when entered in first line of function, will show the
             contents of while declaration or function call as guidelines/instructions
